\documentclass[border=16pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning}
\begin{document}
\begin{tikzpicture}[
    node distance=0.7cm and 1.5cm,
    input/.style={rectangle, draw=black, thick, minimum width=3.8cm, minimum height=0.8cm,
                  text centered, font=\small, rounded corners=2pt},
    process/.style={rectangle, draw=black, thick, minimum width=4.8cm, minimum height=0.9cm,
                    text centered, font=\small\bfseries, fill=gray!6, rounded corners=2pt},
    output/.style={rectangle, draw=black, thick, minimum width=3.8cm, minimum height=0.8cm,
                   text centered, font=\small, rounded corners=2pt, fill=gray!5},
    arr/.style={-{Stealth[length=2mm]}, thick},
]

% Inputs (top row)
\node[input] (dem) {DEM (30 m)};
\node[input, right=1.5cm of dem] (sat) {Satellite texture};
\node[input, right=1.5cm of sat] (sky) {Sky photos / gradients};

% Renderer
\node[process, below=1.2cm of $(dem)!0.5!(sky)$] (render) {PyRender};
\node[font=\footnotesize, below=0.05cm of render] (weather) {6 weather presets};

% Outputs (bottom row)
\node[output, below left=1.0cm and 0.2cm of render] (img) {RGB image};
\node[output, below right=1.0cm and 0.2cm of render] (mask) {Sky mask (ground truth)};

% Arrows
\draw[arr] (dem) -- (render);
\draw[arr] (sat) -- (render);
\draw[arr] (sky) -- (render);
\draw[arr] (render) -- (img);
\draw[arr] (render) -- (mask);

\end{tikzpicture}
\end{document}
